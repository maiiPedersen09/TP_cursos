<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>P&C | Tu Plataforma de Cursos Online</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
      <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-SgOJa3DmI69IUzQ2PVdRZhwQ+dy64/BUtbMJw1MZ8t5HZApcHrRKUc4W0kG879m7" crossorigin="anonymous">
    <link rel="stylesheet" href="<?php echo BASE_URL; ?>css/style.css"> 
    <link rel="stylesheet" href="<?php echo BASE_URL; ?>css/header.css">
    <link rel="stylesheet" href="<?php echo BASE_URL; ?>css/footer.css">
</head>

    <?php
      if (session_status() !== PHP_SESSION_ACTIVE) {
          session_start();
      }
    ?>

<header class="main-header">
    <div class="header-container">
        <a href="<?php echo BASE_URL; ?>listar" class="logo">
            <span class="logo-text">P&C</span> 
        </a>

    <nav class="main-nav">
        <a href="<?php echo BASE_URL; ?>listar">Cursos</a>
            <div class="dropdown">
                <a href="<?php echo BASE_URL; ?>listarCategorias" class="dropbtn">Secciones <i class="fas fa-caret-down"></i></a>
                <div class="dropdown-content">
                    <?php if (isset($categorias) && is_array($categorias)): ?>
                        <?php foreach ($categorias as $cat): ?>
                            <a href="<?php echo BASE_URL; ?>listarPorCategoria/<?php echo $cat->id; ?>">
                                <?php echo ($cat->nombre); ?>
                            </a>
                        <?php endforeach; ?>
                    <?php else: ?>
                        <a href="#">No hay secciones</a>
                    <?php endif; ?>
                </div>
            </div>
            <?php if (!empty($_SESSION['ES_ADMIN'])): ?>
                <a href="<?php echo BASE_URL; ?>mostrarFormDeAñadir">Añadir</a>
                <a href="<?php echo BASE_URL; ?>mostrarFormCat">Añadir Categorías</a>
            <?php endif; ?>
            <a href="#">Contacto</a>
    </nav>





    <!-- CUENTA DE USUARIO (si esta logueado) / LOGIN / REGISTER -->


    <?php if(isset($_SESSION['EMAIL_USER'])): ?>
        <div class="dropdown user-menu">
            <a href="#" class="user-toggle dropdown-toggle" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="bi bi-person-circle"></i>
            <span><?= explode('@', $_SESSION['EMAIL_USER'])[0]; ?></span>
            </a>

            <ul class="dropdown-menu user-dropdown" aria-labelledby="userDropdown">
            <li><small><?= $_SESSION['EMAIL_USER']; ?></small></li>
            <li><hr></li>
            <li><a class="dropdown-item logout" href="Logout">Cerrar sesión</a></li>
            </ul>
        </div>

        <?php else: ?>
        <div class="dropdown user-menu">
            <a href="#" class="user-toggle dropdown-toggle" id="guestDropdown" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="bi bi-person-fill"></i>
            <span>Cuenta</span>
            </a>

            <ul class="dropdown-menu user-dropdown" aria-labelledby="guestDropdown">
            <li><a class="dropdown-item" href="showLogin">Iniciar sesión</a></li>
            <li><a class="dropdown-item" href="showRegister">Registrarse</a></li>
            </ul>
        </div>
        <?php endif; ?>



        <button class="menu-toggle" aria-label="Abrir Menú">
            <i class="fas fa-bars"></i> 
        </button>
    </div>
</header>